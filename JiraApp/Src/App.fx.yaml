App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =ClearCollect(Tokencoll,Filter(APIToken,UserEmail=User().Email));
        Set(varAPItoken,First(Tokencoll).'API Token');
        ClearCollect(menuitems,{Name:"View Work Log History",Screen:Home},
        {Name:"View Work Log History",Screen:'View WorkLog History'});
        ClearCollect(showmenu,{Value: false});

